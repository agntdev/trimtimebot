# booking_flow — Client booking flow: service selection with durations, current week display (Mon–Sun), slot validity check aligned to 30-min grid, transactional slot availability re-check before booking (fixes 7-day window contradiction, 30-min grid constraint, race condition handling)

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 TRIM

Client booking flow: service selection with durations, current week display (Mon–Sun), slot validity check aligned to 30-min grid, transactional slot availability re-check before booking (fixes 7-day window contradiction, 30-min grid constraint, race condition handling)