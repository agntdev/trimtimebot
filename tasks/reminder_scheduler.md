# reminder_scheduler — Daily 10:00 reminder system: send notifications for confirmed appointments, track reminded_at to prevent duplicates, use SHOP_TZ for time rendering (fixes idempotent reminder constraint)

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 TRIM

Daily 10:00 reminder system: send notifications for confirmed appointments, track reminded_at to prevent duplicates, use SHOP_TZ for time rendering (fixes idempotent reminder constraint)